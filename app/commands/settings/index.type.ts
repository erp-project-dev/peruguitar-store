export interface SettingsViewModel {
  publishNumber: string;

  instagram: string;

  whatsappLinks: {
    store: string;
    community: string;
  };

  clarityProjectId: string;

  website: {
    url: string;
    title: string;
    description: string;
  };
}
