export interface SettingsViewModel {
  publishNumber: string;

  publishType: {
    standard: string;
    premium: string;
  };

  instagram: string;

  whatsappLinks: {
    store: string;
    community: string;
  };

  clarityProjectId: string;
}
